<form method="post" action="{{ path('log_routes_delete', {'id': log_route.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ log_route.id) }}">
    <button class="btn">Delete</button>
</form>
