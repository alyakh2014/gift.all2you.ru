{% extends 'base_oneside.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

    <h1>{{ 'Edit Partners' | trans }}</h1>

    <div class="col-sm-12">
        <a href="{{ path('partners_index') }}">{{ 'back to list' | trans }}</a>
    </div>
    <div class="col-sm-12">
        {{ include('partners/_form.html.twig', {'button_label': 'Обновить'}) }}
    </div>
    <div class="col-sm-12">
        {{ include('partners/_delete_form.html.twig') }}
    </div>
{% endblock %}
